spring:
  config:
    activate:
      on-profile: staging
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:marketplace_staging}
    username: ${DB_USERNAME:staging_user}
    password: ${DB_PASSWORD:staging_pass}
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

storage:
  type: s3
  bucket: ${S3_BUCKET:marketplace-staging}
  region: ${S3_REGION:us-east-1}
  access-key: ${S3_ACCESS_KEY}
  secret-key: ${S3_SECRET_KEY}

payfast:
  merchant-id: ${PAYFAST_MERCHANT_ID:10000100} # Default Sandbox ID
  merchant-key: ${PAYFAST_MERCHANT_KEY:46f0cd694581a} # Default Sandbox Key
  passphrase: ${PAYFAST_PASSPHRASE:sandbox}
  sandbox-mode: true
  base-url: https://sandbox.payfast.co.za
